import { DevModeDirective } from './dev-mode.directive';

describe('DevModeDirective', () => {
  it('should create an instance', () => {
    const directive = new DevModeDirective();
    expect(directive).toBeTruthy();
  });
});
